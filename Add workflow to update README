with open(results_file, newline='', encoding='utf-8') as f:
    reader = csv.DictReader(f)
    print("📊 نتائج العمليات / الاختبارات:")
    print("=" * 50)
    for row in reader:
        print(f"نوع العملية : {row['نوع العملية']}")
        print(f"الوصف       : {row['الوصف']}")
        print(f"التاريخ     : {row['التاريخ']}")
        print(f"الساعة      : {row['الساعة']}")
        print(f"المدة       : {row['المدة']}")
        print("-" * 50